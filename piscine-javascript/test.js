/*function id(value) {
    return value;
  }
function getLength(input) {
    if (Array.isArray(input) || typeof input === 'string') {
      return input.length;
    }
    throw new TypeError('The input must be an array or a string.');
}
*/
function hey(value) {
    return  value
}
console.log(hey);
function getLength(input) {
    if (Array.isArray(input) || typeof input === "string") {
        return input.length;
    }
    throw new TypeError('blalalallalalalalalal.');
}
const fruits = ["mango",56,"hry"]
console.log(getLength(fruits));